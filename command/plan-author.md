---
description: Structure gathered context into a .project-plans/ plan document
---

Load the `plan-author` skill and use it to structure the current conversation's gathered context into a plan document.

If an argument is provided, use it as the plan slug (filename identifier). Otherwise, derive a slug from the goal/topic.

Example: `/plan-author config-mcp-scoping`

{{$arguments}}
